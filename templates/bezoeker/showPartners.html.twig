{%  extends 'base.html.twig' %}

{% block headerintro %}

<section class="nieuwsOverzicht">
    <div class="container text-center" >
        <div class="title">
            <div class="row justify-center-md-center">
                <div class="col-md-12 col-lg-12 col-sm-6 ">
                    <h2>Partners</h2>
                </div>
            </div>
        </div>
    </div>
</section>
<div class="container" >
    <section class="partners">
        <div class="row justify-center-md-center">
            {% for partner in partners %}
                    <!--    start  partners -->
                <div class="col-lg-4 col-md-4 col-sm-12 col-xs-12 ">
                   <div class="container">
                        <div class="card">
                            <div class="card-img"> <img class="card-img-top" src="../{{  vich_uploader_asset(partner, 'imageFile') }}" alt="{{ partner.name }}" /></div>
                                 <div class="card-body" >

                                     <h5>{{ partner.name }}</h5>
                                     <a href="{{ partner.website }}"><button >Website</button></a>


                                 </div>
                        </div>
                    </div>
                </div>
            <!--    einde  partners -->
            {% endfor %}
        </div>
    </section>

</div>
</section>
{% endblock %}




